# CutFlow•CAD API — Phase 0 dependencies
# Conservative lower-bound pins; lock exact versions later (Phase 2+).

# Web framework
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
python-multipart>=0.0.9,<1.0

# DXF read/write (Phase 1+)
ezdxf>=1.3,<2.0

# Geometry & offset (Phase 2+)
shapely>=2.0,<3.0
pyclipper>=1.3,<2.0

# PDF output (Phase 3+)
reportlab>=4.1,<5.0

# Tests (kept here so docker image can lint-run the suite if needed)
pytest>=8.0,<9.0
httpx>=0.27,<1.0
